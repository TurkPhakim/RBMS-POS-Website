# ผลการทดสอบระบบ RBMS-POS

> ทดสอบจากโค้ดจริง (Backend + Frontend) ครอบคลุมทุกฟีเจอร์ตาม WorkFlow การใช้งานจริง

---

## 1. Auth Module — เข้าสู่ระบบ & ความปลอดภัย

> ผู้ใช้เข้าสู่ระบบ กู้คืนรหัสผ่าน มีระบบความปลอดภัยและ Session ครบถ้วน

| ลำดับ | ฟีเจอร์ & WorkFlow | ผลลัพธ์ |
|:---:|---|:---:|
| 1 | Login (Username + Password + ReCaptcha) + Logout + Auto Refresh Token | ✅ |
| 2 | ล็อคบัญชีอัตโนมัติหลังกรอกรหัสผิด (Exponential Lockout) + Admin ล็อค/ปลดล็อค | ✅ |
| 3 | ลืมรหัสผ่าน → ส่ง OTP ทาง Email → ยืนยัน → ตั้งรหัสใหม่ (ห้ามซ้ำ 3 รหัสล่าสุด) | ✅ |
| 4 | เปลี่ยนรหัสผ่าน + ยืนยันตัวตนด้วยรหัสผ่านสำหรับ Action สำคัญ | ✅ |
| 5 | Session Timeout — หมดเวลาใช้งานอัตโนมัติ + นับถอยหลัง + ต่ออายุ/ออกจากระบบ | ✅ |

---

## 2. Profile Module — โปรไฟล์พนักงาน

> พนักงานดู/แก้ไขข้อมูลส่วนตัว จัดการข้อมูลเพิ่มเติม และตั้งค่า PIN

| ลำดับ | ฟีเจอร์ & WorkFlow | ผลลัพธ์ |
|:---:|---|:---:|
| 1 | ดูข้อมูลส่วนตัว + แก้ไขข้อมูลที่อนุญาต (ชื่อเล่น, Line, เบอร์โทร, บัญชีธนาคาร) | ✅ |
| 2 | อัพโหลด / ลบรูปโปรไฟล์ | ✅ |
| 3 | จัดการที่อยู่ / ประวัติการศึกษา / ประวัติการทำงาน (CRUD หลายรายการ) | ✅ |
| 4 | เปลี่ยนรหัสผ่าน (จาก User Menu) | ✅ |
| 5 | ตั้งค่า / เปลี่ยน / รีเซ็ต PIN 6 หลัก | ✅ |

---

## 3. Dashboard Module — แดชบอร์ดภาพรวมร้าน

> แสดงภาพรวมยอดขายด้วย KPI Cards, กราฟ, สถิติเมนูขายดี

| ลำดับ | ฟีเจอร์ & WorkFlow | ผลลัพธ์ |
|:---:|---|:---:|
| 1 | KPI Cards 6 ใบ (ยอดขาย, ออเดอร์, ลูกค้า, เฉลี่ย/ออเดอร์, กำไร, %กำไร) | ✅ |
| 2 | กราฟ 4 แบบ (แนวโน้มรายได้, เปรียบเทียบช่วงเวลา, สัดส่วนครัว, ชั่วโมงขายดี) | ✅ |
| 3 | Top 5 เมนูขายดี (แยก อาหาร / เครื่องดื่ม / ของหวาน) | ✅ |
| 4 | รายงานยอดขายตามช่วงวันที่ + เลือกวันที่กรอง + สลับ 7/30 วัน | ✅ |

---

## 4. Order Module — จัดการออเดอร์

> จัดการออเดอร์ครบวงจร ตั้งแต่เปิดโต๊ะจนถึงขอบิล

| ลำดับ | ฟีเจอร์ & WorkFlow | ผลลัพธ์ |
|:---:|---|:---:|
| 1 | ดูภาพรวมร้าน (ผังโต๊ะ Real-time) + รายการออเดอร์ (Pagination + Filter) | ✅ |
| 2 | สร้าง Order + เพิ่มรายการอาหาร (เมนู + ตัวเลือก + จำนวน) + ส่งครัว Real-time | ✅ |
| 3 | Status Flow: Pending → Cooking → Ready → Served → Completed | ✅ |
| 4 | Void / Cancel รายการ (พร้อมเหตุผล) | ✅ |
| 5 | ขอบิล + ส่ง/ยกเลิกบิล + แยกบิล (ตามรายการ / ตามจำนวนเงิน) + ยกเลิกแยก | ✅ |
| 6 | รวมโต๊ะ + ย้ายโต๊ะ (โอนข้อมูล + สร้าง QR ใหม่ + แจ้งเตือน) | ✅ |

---

## 5. Menu Module — จัดการเมนู

> จัดการเมนูอาหาร เครื่องดื่ม ของหวาน รวมถึงหมวดหมู่และตัวเลือกเสริม

| ลำดับ | ฟีเจอร์ & WorkFlow | ผลลัพธ์ |
|:---:|---|:---:|
| 1 | ดูรายการเมนูแยกประเภท + ค้นหา + กรองสถานะ (Permission แยกแต่ละประเภท) | ✅ |
| 2 | CRUD เมนู (ชื่อ, ราคา, ต้นทุน, หมวดหมู่, รูปภาพ, ตัวเลือกเสริม) | ✅ |
| 3 | จัดการหมวดหมู่ย่อย CRUD + จัดเรียง Drag & Drop | ✅ |
| 4 | จัดการกลุ่มตัวเลือก + รายการตัวเลือก CRUD + เชื่อมกับเมนู | ✅ |

---

## 6. Table Module — จัดการโต๊ะ & การจอง

> จัดการโซน โต๊ะ ออกแบบผังร้าน และจัดการการจอง

| ลำดับ | ฟีเจอร์ & WorkFlow | ผลลัพธ์ |
|:---:|---|:---:|
| 1 | จัดการโซน CRUD + จัดเรียงลำดับ | ✅ |
| 2 | จัดการโต๊ะ CRUD + สถานะ + เปิด/ปิด + QR Token + รวม/แยกโต๊ะ | ✅ |
| 3 | ออกแบบผังร้าน Drag & Drop (โต๊ะ + วัตถุตกแต่ง) | ✅ |
| 4 | จองโต๊ะ CRUD + Status Flow (Pending → Confirmed → CheckedIn → Complete / Cancel / NoShow) + ปฏิทิน | ✅ |

---

## 7. Payment Module — ชำระเงิน & ใบเสร็จ

> จัดการรอบขาย ชำระเงินทั้งเงินสดและ QR ตรวจสลิปอัตโนมัติ ออกใบเสร็จ

| ลำดับ | ฟีเจอร์ & WorkFlow | ผลลัพธ์ |
|:---:|---|:---:|
| 1 | เปิด/ปิดรอบขาย + เงินเข้า/ออกลิ้นชัก + สรุปยอด | ✅ |
| 2 | ชำระเงินสด (คำนวณทอนอัตโนมัติ) | ✅ |
| 3 | ชำระ QR + อัพโหลดสลิป + Slip OCR ตรวจอัตโนมัติ + แคชเชียร์ยืนยัน | ⚠️ |
| 4 | ประวัติชำระเงิน + ประวัติรอบขาย + รายละเอียดรอบขาย | ✅ |
| 5 | ออกใบเสร็จ / ดาวน์โหลด (รายบิล + สรุปรวม) | ✅ |

> **⚠️ ข้อจำกัด Slip OCR:** ระบบตรวจสลิปอัตโนมัติทำงานได้กับสลิปที่เป็น Screenshot จากแอปธนาคาร แต่ **ไม่สามารถตรวจสลิปที่ถ่ายจากหน้าจอมือถือ (ภาพถ่าย) ได้** เนื่องจากคุณภาพภาพ, มุมเอียง, แสงสะท้อน ทำให้ OCR อ่านข้อมูลผิดพลาด — กรณีนี้แคชเชียร์ต้องตรวจสอบและยืนยันด้วยตนเอง

---

## 8. Kitchen Display Module — หน้าจอครัว

> แสดงออเดอร์ที่รอทำ Real-time แยกประเภท ให้พ่อครัวอัพเดตสถานะได้

| ลำดับ | ฟีเจอร์ & WorkFlow | ผลลัพธ์ |
|:---:|---|:---:|
| 1 | แสดงรายการรอทำแยกประเภท (อาหาร / เครื่องดื่ม / ของหวาน) + 2 มุมมอง (Order View / Menu View) | ✅ |
| 2 | อัพเดตสถานะ: เริ่มทำ → ทำเสร็จ + Real-time ทั้งรับและส่ง (SignalR) | ✅ |
| 3 | แสดงข้อมูลโต๊ะ/โซน + KPI สรุป + Permission แยกตาม Category | ✅ |

---

## 9. Human Resource Module — ทรัพยากรบุคคล

> จัดการข้อมูลพนักงานครบถ้วน รวมถึงสร้างบัญชีผู้ใช้

| ลำดับ | ฟีเจอร์ & WorkFlow | ผลลัพธ์ |
|:---:|---|:---:|
| 1 | ดูรายชื่อพนักงาน (Pagination + กรอง + ค้นหา) | ✅ |
| 2 | CRUD พนักงาน (ข้อมูลส่วนตัว + การจ้าง + ติดต่อ + รูปโปรไฟล์) | ✅ |
| 3 | จัดการที่อยู่ / ประวัติการศึกษา / ประวัติการทำงาน (CRUD หลายรายการต่อคน) | ✅ |
| 4 | สร้างบัญชีผู้ใช้ (Auto-generate Username / Password) | ✅ |

---

## 10. Admin Setting Module — ตั้งค่าระบบ

> จัดการผู้ใช้ ตำแหน่ง สิทธิ์การเข้าถึง ตั้งค่าร้าน และค่าบริการ

| ลำดับ | ฟีเจอร์ & WorkFlow | ผลลัพธ์ |
|:---:|---|:---:|
| 1 | จัดการผู้ใช้งาน (ดู + แก้สถานะ + ล็อค/ปลดล็อค + รีเซ็ต Failed Login) | ✅ |
| 2 | จัดการตำแหน่ง CRUD + Permission Matrix (Tree + Checkbox) + Module Tree | ✅ |
| 3 | ตั้งค่าร้านค้า (ข้อมูลร้าน + โลโก้ + QR ชำระเงิน + เวลาเปิด-ปิด) | ✅ |
| 4 | จัดการค่าบริการ CRUD (ชื่อ, เปอร์เซ็นต์, สถานะ) | ✅ |

---

## 11. Mobile Web — ลูกค้าสั่งอาหารเอง

> ลูกค้าสแกน QR → ดูเมนู → สั่งอาหาร → ติดตามสถานะ → ชำระเงิน → รับใบเสร็จ

| ลำดับ | ฟีเจอร์ & WorkFlow | ผลลัพธ์ |
|:---:|---|:---:|
| 1 | สแกน QR ที่โต๊ะ → Session Token + ตั้งชื่อเล่น | ✅ |
| 2 | เรียกดูเมนูแยกหมวด + ค้นหา + ดูรายละเอียด + เลือกตัวเลือกเสริม | ✅ |
| 3 | ตะกร้า (เพิ่ม / แก้ไข / ลบ) + สั่งอาหาร → ส่งครัว | ✅ |
| 4 | ติดตามสถานะ Real-time + เรียกพนักงาน (Cooldown 60 วินาที) | ✅ |
| 5 | ขอบิล + รอบิล (Auto-navigate) + ดูสรุปบิล | ✅ |
| 6 | ชำระเงินสด / QR + อัพโหลด Slip + หารบิลเท่า / หารตามรายการ | ✅ |
| 7 | รอตรวจ Slip → ชำระสำเร็จ → ดาวน์โหลดใบเสร็จ | ✅ |
| 8 | หน้าร้านปิด + หน้า Token หมดอายุ | ✅ |

---

## 12. Notification System — แจ้งเตือน Real-time

> แจ้งเตือน Real-time ตามสิทธิ์ของแต่ละตำแหน่ง

| ลำดับ | ฟีเจอร์ & WorkFlow | ผลลัพธ์ |
|:---:|---|:---:|
| 1 | รับแจ้งเตือน Real-time (SignalR) + Toast + Notification Drawer + Badge | ✅ |
| 2 | อ่านแจ้งเตือน + กรอง 9 ประเภท | ✅ |
| 3 | แยกกลุ่มรับ: Kitchen / Floor / Cashier / Manager (เห็นเฉพาะที่เกี่ยวข้อง) | ✅ |

---

## 13. Base Web — โครงสร้างพื้นฐาน

> Layout หลักที่รองรับทุกโมดูล พร้อมระบบ Guard และ Lazy Loading

| ลำดับ | ฟีเจอร์ & WorkFlow | ผลลัพธ์ |
|:---:|---|:---:|
| 1 | Sidebar 8 เมนูหลัก + 22 เมนูย่อย (แสดงตามสิทธิ์) + พับได้ | ✅ |
| 2 | Header (โลโก้ + ชื่อร้านจาก API) + Notification Badge + User Menu | ✅ |
| 3 | Breadcrumb + Action Buttons + Global Loading Overlay อัตโนมัติ | ✅ |
| 4 | Auth Guard + Permission Guard + Guest Guard + Access Denied Page | ✅ |
| 5 | Lazy Loading ทุก Feature Module | ✅ |

---

## สรุปผลทดสอบรวมทั้งระบบ

| โมดูล | จำนวนฟีเจอร์ | ผลลัพธ์ |
|:---:|:---:|:---:|
| Auth | 12 | ✅ ผ่านครบ |
| Profile | 10 | ✅ ผ่านครบ |
| Dashboard | 10 | ✅ ผ่านครบ |
| Order | 18 | ✅ ผ่านครบ |
| Menu | 13 | ✅ ผ่านครบ |
| Table | 13 | ✅ ผ่านครบ |
| Payment | 12 | ⚠️ Slip OCR มีข้อจำกัด |
| Kitchen Display | 9 | ✅ ผ่านครบ |
| Human Resource | 10 | ✅ ผ่านครบ |
| Admin Setting | 11 | ✅ ผ่านครบ |
| Mobile Web | 21 | ✅ ผ่านครบ |
| Notification | 10 | ✅ ผ่านครบ |
| Base Web | 13 | ✅ ผ่านครบ |
| **รวมทั้งหมด** | **162** | **⚠️ 161 ผ่าน / 1 มีข้อจำกัด** |

---

## ปัญหาที่พบระหว่างพัฒนาและแนวทางแก้ไข

### 1. SignalR Connection หลุดแล้วไม่ได้รับข้อมูล Real-time

**ปัญหา:**
ระบบใช้ SignalR Group สำหรับส่งข้อมูล Real-time เช่น Mobile Web join group `table_5`, Client join group `floor` เมื่อ server ส่ง event จะส่งไปหา group ที่เกี่ยวข้อง เมื่อ internet สะดุดแม้แค่ชั่วขณะ SignalR จะ disconnect แล้ว reconnect อัตโนมัติผ่าน `withAutomaticReconnect()` แต่ **group membership หายไป** เพราะ server ไม่จำ group ให้หลัง reconnect ทำให้ client ต่อกลับได้แต่ไม่ได้อยู่ใน group → ไม่ได้รับ event อะไรอีกเลย จนกว่าจะ refresh หน้า

**แก้ไข:**
- เพิ่ม `onreconnected` callback — เมื่อต่อกลับสำเร็จ → **rejoin group อัตโนมัติ** → กลับมารับ event ได้ทันทีโดยไม่ต้อง refresh
- เพิ่ม `onclose` callback — ถ้า reconnect ล้มเหลวทุกรอบ (เน็ตหลุดนาน) → connection ตายถาวร → **reset state ทั้งหมด** (`started`, `hubConnection`, `joinedGroups`) → ครั้งถัดไปที่เข้าหน้าใหม่จะสร้าง connection ใหม่ได้เลย ไม่ค้างอยู่กับ connection ที่ตายแล้ว
- เพิ่ม listeners ที่ขาดไป (`ItemCancelled`, `SlipUploaded`) ให้ครบทุก event

---

### 2. Race Condition ใน SignalR Service

**ปัญหา:**
ระบบมีหลาย Component ที่ต้องใช้ SignalR พร้อมกัน (เช่น OrderHub ใช้ทั้งหน้า Order List, Kitchen Display, Notification) เมื่อหลาย Component เรียก `start()` พร้อมกัน เช่น Component A กำลัง `await hubConnection.start()` อยู่ → Component B เรียก `start('floor')` ซ้ำ → B ข้าม await ไป `invoke('JoinGroup')` ก่อนที่ connection จะ ready → **fail เงียบ** ทำให้บาง Component ไม่ได้ join group และไม่ได้รับข้อมูล

**แก้ไข:**
- เพิ่ม `connectingPromise` เป็น lock กลางใน `OrderHubService.start()` — เมื่อ Component B เจอว่ามี `connectingPromise` อยู่แล้ว → รอให้ connection ของ A เสร็จก่อน → แล้วค่อย JoinGroup สำเร็จ
- เพิ่ม guard `if (!this.started) return;` ก่อน JoinGroup ป้องกันกรณี connection fail
- แก้ `NotificationSignalRService.connect()` เช่นกัน เพราะมีปัญหาเดียวกัน — Call B เข้ามาขณะ Call A กำลัง await → สร้าง connection ทับ → connection เก่า orphaned

---

### 3. OCR ไม่ทำงานบน Production Server (Linux)

**ปัญหา:**
ระบบตรวจสลิปอัตโนมัติ (Slip OCR) ใช้ NuGet package `Tesseract 5.2.0` (.NET wrapper) ซึ่งทำงานได้ปกติบน Windows (Development) แต่เมื่อ deploy ขึ้น Production Server ที่เป็น Debian 12 (Linux) เกิด error `LeptonicaApi.Initialize()` เพราะ **native library version ไม่ตรงกัน** — NuGet wrapper ถูก build สำหรับ Leptonica 1.82.x แต่ Debian 12 มี Leptonica 1.84.x

นอกจากนี้ยังพบว่า Tesseract OCR อ่านได้เฉพาะสลิปที่เป็น **Screenshot จากแอปธนาคารโดยตรง** แต่สลิปที่ **ถ่ายจากกล้องมือถือ** อ่านไม่ได้ เพราะภาพถ่ายมี noise สูง ตัวอักษรไทยเบลอ Tesseract แปลงเป็นข้อความที่ไม่มีความหมาย (เช่น `wees`, `bce`, `umn`) ไม่สามารถดึงวันที่หรือเลขบัญชีได้

**แก้ไข:**
- เปลี่ยนจาก NuGet wrapper → **เรียก Tesseract CLI ผ่าน `Process.Start`** แทน เพราะ CLI ถูก compile พร้อมกับ native library ที่ตรง version กัน ไม่มีปัญหา P/Invoke อีก
- ปรับ Dockerfile ให้ติดตั้ง `tesseract-ocr` + `tesseract-ocr-tha` (Thai language pack) แทน `libtesseract-dev`
- เพิ่ม image preprocessing ด้วย `-normalize` เพื่อเพิ่ม contrast โดยไม่ทำลายรายละเอียดตัวเลข
- เพิ่ม `NormalizeThaiText()` — ลบช่องว่างระหว่างตัวอักษรไทยที่ OCR ใส่ผิด เช่น `มี . ค .` → `มี.ค.`, `บ า ท` → `บาท`
- **ข้อจำกัดที่เหลือ**: สลิปที่ถ่ายจากกล้องมือถือยังอ่านไม่ได้ แนะนำให้ลูกค้า Screenshot จากแอปธนาคารโดยตรง
