# CLAUDE.md — โหมดเขียนรายงานปริญญานิพนธ์

> **โหมดปัจจุบัน**: 📝 **THESIS WRITING ONLY** — โปรเจคนี้พัฒนาโค้ดเสร็จแล้ว ขั้นต่อไปคือเขียนรายงานปริญญานิพนธ์
>
> **อัพเดตล่าสุด**: 2026-05-30
>
> หากต้องการกลับโหมดพัฒนาโค้ด — ดู [CLAUDE-dev.md](CLAUDE-dev.md) (กฎพัฒนาโค้ดเดิมทั้งหมด)

---

## 🎯 โหมดการทำงาน

**โฟกัสเดียว**: ช่วยผู้ใช้เขียนรายงานปริญญานิพนธ์ของระบบ RBMS-POS

**สิ่งที่ "ห้ามทำ" ในโหมดนี้** (จนกว่าผู้ใช้จะสั่งให้กลับโหมดพัฒนา):

- ❌ ห้ามแก้ไขโค้ดใน `Backend-POS/` หรือ `Frontend-POS/`
- ❌ ห้ามรัน Backend / Frontend / Docker
- ❌ ห้ามสร้าง Entity / Migration / API / Controller / Component ใหม่
- ❌ ห้ามรัน `npm run gen-api`, `dotnet ef`, `ng serve`, `dotnet run`
- ❌ ห้ามสร้าง Task file ของฟีเจอร์โค้ดใหม่ใน `doc/tasks/`
- ❌ ห้ามแนะนำให้ "เพิ่มฟีเจอร์ใหม่" หรือ "refactor โค้ด"

**สิ่งที่ "ทำได้" ในโหมดนี้**:

- ✅ อ่านโค้ดเพื่อยืนยันความถูกต้องของเนื้อหาในรายงาน
- ✅ อ่าน/แก้ไข/สร้างไฟล์ใน `doc/report/`, `doc/references/`, `doc/modules/`
- ✅ ค้นข้อมูลจาก Web (เช่น Citation Style, สำนวน, อ้างอิงทางการ)
- ✅ ช่วยตั้งชื่อรูป/ตาราง + เขียน Caption
- ✅ ช่วยแปลภาษา (ไทย ↔ อังกฤษ) สำหรับ Abstract
- ✅ ช่วยตรวจสำนวน + ไวยากรณ์

---

## 📁 โครงสร้างไฟล์รายงาน

```
doc/
├── references/          ← แม่แบบและสำนวนจากปริญญานิพนธ์ตัวอย่าง (READ-ONLY)
│   ├── README.md
│   ├── chapter-1-introduction.md
│   ├── chapter-2-theory.md
│   ├── chapter-3-design.md
│   ├── chapter-4-results.md
│   ├── chapter-5-conclusion.md
│   ├── user-manual.md
│   └── writing-style-guide.md
│
├── report/              ← ไฟล์รายงานจริงของ RBMS-POS (กรอกเนื้อหา)
│   ├── README.md                              ← Index
│   ├── 01-abstract-acknowledgment.md          ← บทคัดย่อ + กิตติกรรม
│   ├── 02-figure-table-index.md               ← สารบัญรูปและตาราง
│   ├── 03-chapter-1-introduction.md           ← บทที่ 1 บทนำ
│   ├── 04-chapter-2-theory.md                 ← บทที่ 2 ทฤษฎี + IEEE References
│   ├── 05-chapter-3-design.md                 ← บทที่ 3 การออกแบบ
│   ├── 06-chapter-4-results.md                ← บทที่ 4 ผลการทดลอง
│   ├── 07-chapter-5-conclusion.md             ← บทที่ 5 บทสรุปผล
│   ├── 08-user-manual.md                      ← คู่มือการใช้งาน
│   └── 09-questions-for-author.md             ← 20 คำถามที่ผู้ใช้ต้องตอบก่อนเขียน
│
└── modules/             ← สรุป 8 Business Modules + Workflow + Pros/Cons
    ├── README.md
    └── 01-08-*.md
```

**ไฟล์อ้างอิงข้อมูลระบบสำหรับเขียนรายงาน**:
- `doc/modules/` — Business Logic + Workflow ของแต่ละ Module (8 ไฟล์)
- `doc/features/project-status.md` — สถานะโปรเจคและ Migration timeline
- `doc/architecture/database-api-reference.md` — Database schema + API endpoints

---

## ✍️ กฎการเขียน

### ภาษา
- **ตอบเป็นภาษาไทยเสมอ** — ทุก response ใช้ภาษาไทยเป็นหลัก
- **ทุก .md ในรายงานเป็นภาษาไทย** — heading คำอธิบาย comment ทุกอย่าง
- คำศัพท์เทคนิคที่ไม่มีคำแปลที่ดี (Entity, SignalR, JWT, OCR, ฯลฯ) ใช้ภาษาอังกฤษได้

### สำนวน (อ้างอิงจาก `doc/references/writing-style-guide.md`)
- **ไม่ใช้สรรพนามบุคคลที่ 1** — ใช้ "คณะผู้จัดทำ" หรือ "ผู้จัดทำ" แทน "ผม/ฉัน/เรา"
- **ใช้รูปแบบ "ภาษาไทย (English)"** สำหรับคำศัพท์เทคนิคครั้งแรก เช่น "การยืนยันตัวตน (Authentication)"
- **หลีกเลี่ยงคำพูดในชีวิตประจำวัน** — ห้ามใช้ "เยอะ", "ง่าย ๆ", "โอเค", "ใช่ไหม"
- **เขียนเชิงวิชาการแบบเป็นทางการ** — ประโยคยาว มีคำเชื่อม "อย่างไรก็ตาม", "ดังนั้น", "ซึ่ง"

### รูปแบบ
- **รูป**: "รูปที่ X.Y ..." (X = บทที่, Y = ลำดับในบท) — อ้างอิงในเนื้อหาว่า "ดังแสดงในรูปที่ X.Y"
- **ตาราง**: "ตารางที่ X.Y ..." — อ้างอิงว่า "ดังแสดงในตารางที่ X.Y"
- **บรรณานุกรม**: IEEE Citation Style — `[N]` ในเนื้อหา + รายการท้ายไฟล์
- **ที่มาของรูปในบทที่ 2**: "(ที่มา: URL)" ใต้คำบรรยายภาพ

---

## 🔄 Workflow การเขียนรายงาน

### ขั้นที่ 1: ตอบคำถาม 20 ข้อ
ผู้ใช้เปิด [doc/report/09-questions-for-author.md](doc/report/09-questions-for-author.md) แล้วตอบทีละข้อ — ไม่ต้องตอบครบในครั้งเดียว

### ขั้นที่ 2: AI กรอกเนื้อหาจริงตามคำตอบ
เมื่อผู้ใช้ตอบคำถามในหมวดใดเสร็จ → แจ้ง AI ให้นำคำตอบไปกรอกในไฟล์รายงานที่เกี่ยวข้อง (เช่น หมวด A → ไฟล์ 01, หมวด B → ไฟล์ 03)

### ขั้นที่ 3: ตรวจสอบ + ปรับสำนวน
- AI ตรวจสำนวนตาม `writing-style-guide.md`
- เปรียบเทียบกับ `doc/references/` ว่าใช้ pattern ตรงกัน

### ขั้นที่ 4: เตรียม Screenshot และรูปประกอบ
- ผู้ใช้เก็บ Screenshot ระบบจริง
- AI ช่วยตั้งชื่อรูปและจัดลำดับใน [02-figure-table-index.md](doc/report/02-figure-table-index.md)

### ขั้นที่ 5: Export เป็น Word/PDF
- AI ช่วยจัดสำนวนสุดท้ายก่อน Export
- ผู้ใช้นำเนื้อหาจาก `.md` ไปวางในเทมเพลตรายงานของสถาบัน

---

## 🎯 หลักการทำงานในโหมดนี้

### 1. Source of Truth — `09-questions-for-author.md` คือคำตอบของผู้จัดทำที่ล็อคไว้แล้ว

**🔴 กฎเหล็กระดับ HARD BLOCKER**:

**ก่อนเขียน/แก้ไฟล์รายงาน (01-08) ทุกครั้ง → ต้องอ่าน [doc/report/09-questions-for-author.md](doc/report/09-questions-for-author.md) ก่อนเสมอ**

ไฟล์ 09 คือ **คำตอบของผู้จัดทำที่ล็อคไว้แล้ว** ใช้เป็นแหล่งข้อมูลหลักในการเขียนเนื้อหารายงาน — มีโครงสร้าง 3 ส่วน:

1. **Quick Reference** (ตารางสรุป) — ใช้ดูภาพรวมเร็ว
2. **Decisions Locked** (จัดกลุ่ม A-I) — ใช้อ้างอิงจริงตอนเขียน
3. **Open Questions** — สิ่งที่ยังไม่ตอบ ใช้รู้ว่าจุดไหนต้องถามเพิ่ม

**ลำดับการดึงข้อมูล** (เช็คตามลำดับนี้ก่อนเขียนทุกย่อหน้า):

1. 🔴 **`doc/report/09-questions-for-author.md`** ← **อันดับ 1 เสมอ** คำตอบของผู้จัดทำที่ล็อคไว้
2. `doc/modules/` — Business Logic + Workflow ของ 8 Module
3. `doc/features/project-status.md` — สถานะระบบ + Migration timeline
4. `doc/architecture/database-api-reference.md` — Entity + API endpoints
5. `doc/architecture/system-overview.md` — N-Tier + SignalR + Auth
6. `doc/architecture/*.md` อื่น ๆ — Design system, Icon, File management
7. `doc/development/*.md` — ถ้าต้องอ้าง coding pattern
8. `doc/references/` — แม่แบบสำนวน (ไม่ใช่แหล่งข้อมูลระบบ)

**ห้ามทำ**:
- ❌ **ถามคำถามที่ผู้ใช้ตอบไว้แล้วในไฟล์ 09** — ใช้คำตอบในไฟล์เลย
- ❌ **เดาคำตอบ** — แม้ว่าจะ "น่าจะใช่" ถ้าไม่มีในไฟล์ 09 → ดูส่วน Open Questions ก่อน
- ❌ เพิ่มฟีเจอร์ที่ระบบไม่มี (เช่น เขียนว่า "รองรับ Delivery" ทั้งที่ระบบรองรับแค่ Dine-in)
- ❌ ใส่ตัวเลขสุ่ม (เช่น "ลดเวลา 50%") โดยไม่มีหลักฐานในไฟล์ 09 หรือ doc/
- ❌ อ้างอิงเทคโนโลยีที่ไม่ได้ใช้ในระบบ
- ❌ คัดลอกข้อความจากแม่แบบ `doc/references/` ตรง ๆ ไปใส่ในรายงาน

**ต้องทำ**:
- ✅ **เริ่มทุก session เขียนรายงานด้วยการอ่านไฟล์ 09 ก่อน** เพื่อ refresh คำตอบล่าสุด
- ✅ ใช้ค่าใน "Decisions Locked" เป็น **fact** ที่ไม่ต้องตรวจสอบเพิ่ม (ผู้ใช้ confirm แล้ว)
- ✅ เมื่อผู้ใช้บอก "แก้ X" → แก้ในไฟล์ 09 ก่อน แล้วค่อยปรับไฟล์รายงานตาม
- ✅ เมื่อต้องการเขียนข้อมูลที่ยังไม่อยู่ในไฟล์ 09 → เพิ่มเข้าใน Open Questions แล้วถามผู้ใช้

**ถ้าข้อมูลไม่อยู่ในไฟล์ 09 และไม่อยู่ใน doc/**:
- เช็ค **Open Questions** ในไฟล์ 09 ก่อน
- ถ้ายังไม่มี → เพิ่มคำถามใหม่ในส่วน Open Questions + ถามผู้ใช้
- **ห้ามเดาเด็ดขาด** — แม้ว่าจะดู "เป็นไปได้" หรือ "น่าจะใช่"

### 1.5. การเปลี่ยน Decisions ของผู้ใช้

**กรณีผู้ใช้สั่งเปลี่ยนคำตอบ** (เช่น "เปลี่ยนชื่อโปรเจคเป็น X" หรือ "ใช้ 5 บทบาทแทน 3 บทบาท"):

1. **อัพเดตไฟล์ 09 ก่อน** (ส่วน Decisions Locked + Q&A Summary + Quick Reference)
2. **ค้นหาทุกที่ในไฟล์รายงาน 01-08** ที่ใช้ข้อมูลเดิม → ปรับให้ตรงกับคำตอบใหม่
3. **แจ้งผู้ใช้** ว่าแก้ที่ไหนบ้าง

**ห้ามแก้ไฟล์รายงานโดยไม่อัพเดตไฟล์ 09 ด้วย** — เพราะจะทำให้ข้อมูลในไฟล์ 09 ล้าสมัย

### 2. ไม่โอเวอร์ — ใช้คำที่เหมาะสมกับขนาดของระบบจริง

รายงานปริญญานิพนธ์ไม่ใช่โฆษณาสินค้า ต้องเขียนตามจริง ไม่เกินจริง

**ห้ามใช้คำเกินจริง**:
- ❌ "ระบบที่ดีที่สุดในตลาด" / "ดีกว่า POS อื่น ๆ"
- ❌ "ใช้งานได้กับทุกร้าน" / "รองรับลูกค้าได้ไม่จำกัด"
- ❌ "ปฏิวัติวงการ" / "นวัตกรรมใหม่ที่ไม่เคยมี"
- ❌ "Real-time แบบสมบูรณ์" (latency 0 ms) — เขียนเป็น "Real-time latency ต่ำกว่า 1 วินาที" แทน
- ❌ "ปลอดภัยที่สุด" — เขียน "ใช้ JWT + BCrypt ตามมาตรฐานทั่วไป" แทน

**ใช้คำที่เหมาะสม**:
- ✅ "พัฒนาขึ้นเพื่อ..." (ไม่ใช่ "สร้างมาเพื่อแก้ปัญหาให้กับทุกร้านอาหาร")
- ✅ "ในกรณีที่ทดสอบ..." (ไม่ใช่ "ทุกสถานการณ์")
- ✅ "ระบบรองรับ X concurrent users ตามผลการทดสอบ" (ไม่ใช่ "รองรับผู้ใช้ไม่จำกัด")
- ✅ "เป็นทางเลือกหนึ่ง" หรือ "สามารถนำมาใช้ทดแทนระบบเดิมในร้านขนาด X-Y โต๊ะ"

**ขอบเขตทางวิชาการ**:
- ตัวเลขที่อ้างใน 5.2 อภิปรายผล ต้องมีที่มาจากการทดสอบจริง (บทที่ 4) — ไม่ใช่การคาดเดา
- ข้อเสนอแนะใน 5.4 ต้องไม่อ้างว่า "จะทำให้ระบบสมบูรณ์" — เขียนว่า "เพื่อให้ระบบครอบคลุมการใช้งานในขอบเขตที่กว้างขึ้น"

### 3. ไม่ซ้ำซ้อน — แต่ละหัวข้อมี Role ของตัวเอง

**แต่ละหัวข้อในรายงานต้องไม่พูดสิ่งเดียวกันซ้ำ** — ถ้ามีข้อมูลที่เกี่ยวข้องในหลายบท ให้แบ่งบทบาทแต่ละบทอย่างชัดเจน:

| ข้อมูลเดียวกัน | พูดในบทไหน | พูดอย่างไร |
|----------------|------------|------------|
| Self-Order QR | 1.1 (ที่มา) | เป็นแนวทางแก้ปัญหา (ทำไมเลือกทำ) |
|  | 1.3 (ขอบเขต) | เป็นฟังก์ชันหนึ่งของระบบ (รายการ bullet สั้น) |
|  | 3.2.3 (Flowchart) | เป็นขั้นตอนการทำงาน (รายละเอียดเชิงระบบ) |
|  | 4.6 (ทดลอง) | เป็นผลการทดสอบจริง (พร้อม Screenshot) |
|  | 5.1 (สรุปผล) | เป็นผลสำเร็จที่ทำได้ (1 bullet สั้น) |
| **บอก 1 ครั้ง 1 มุม — ไม่ซ้ำ** |

**กฎป้องกันซ้ำ**:
- บทที่ 1.2 (วัตถุประสงค์) → ใช้คำว่า "เพื่อ..." (action)
- บทที่ 1.4 (ประโยชน์) → ใช้คำว่า "ช่วยให้..." หรือ "ทำให้..." (result)
- บทที่ 5.1 (สรุปผล) → ใช้คำว่า "สามารถ...ได้" (achievement) — ไม่ใช่ทำซ้ำกับ 1.2
- บทที่ 5.2 (อภิปรายผล) → เน้นผลกระทบเชิงปริมาณ (มีตัวเลข) — ไม่ใช่ทำซ้ำกับ 5.1

**Style ไม่ซ้ำ**:
- รายการ bullet ในแต่ละหัวข้อย่อยใช้ verb เริ่มต้นที่ต่างกัน (ไม่ใช้ "ระบบสามารถ..." 5 ข้อติด)
- ในย่อหน้าเดียวกัน ห้ามใช้คำเดียวกัน 2 ครั้ง — ใช้คำพ้องความหมาย (เช่น "ระบบ" → "แอปพลิเคชัน" → "โครงงาน")

### 4. เหมาะสมกับรายงานปริญญานิพนธ์ภาษาไทย

**รูปแบบทางการ**:
- ใช้ภาษาไทยเป็นหลัก — คำเทคนิคใส่ภาษาอังกฤษในวงเล็บครั้งแรก
- ความยาวประโยค: 1-3 บรรทัด/ประโยค (ไม่สั้นจนเหมือนคำพูด ไม่ยาวจนสับสน)
- ใช้คำเชื่อม: "อย่างไรก็ตาม", "ดังนั้น", "ซึ่ง", "เนื่องจาก", "นอกจากนี้"
- ใช้ passive voice ที่เหมาะสม: "ได้รับการพัฒนา", "ถูกออกแบบเพื่อ"

**สัดส่วนเนื้อหาที่เหมาะสม**:
| บท | ความยาวที่เหมาะสม | จุดที่ต้องระวัง |
|----|--------------------|------------------|
| บทที่ 1 บทนำ | 5-8 หน้า | 1.1 อย่ายาวเกิน 2 หน้า — ปัญหา + แนวทาง พอ |
| บทที่ 2 ทฤษฎี | 12-20 หน้า | แต่ละ tech 1-2 ย่อหน้า + รูป Logo |
| บทที่ 3 การออกแบบ | 20-30 หน้า | Data Dictionary ใส่ Core 8-10 ตาราง เท่านั้น |
| บทที่ 4 ผลการทดลอง | 50-80 หน้า | รูปประกอบเยอะได้ — แต่ละ Step 1 ย่อหน้าสั้น |
| บทที่ 5 บทสรุปผล | 3-5 หน้า | ไม่ใช่บทยาว — 4 หัวข้อย่อยกระชับ |
| คู่มือ | 8-15 หน้า | Step-by-Step + อ้างรูปบทที่ 4 |

**ห้ามเขียน**:
- ❌ ลิสต์ฟีเจอร์เป็นข้อ ๆ ทั้งย่อหน้า (เขียนเป็นย่อหน้าธรรมดา)
- ❌ Code snippet ในเนื้อหา (ถ้าจำเป็นต้องใส่ใน "ภาคผนวก")
- ❌ Emoji — ห้ามเด็ดขาดในรายงานทางการ (ใน .md ของ doc/report/ ใช้เป็น marker ได้ แต่ตอนแปลงเป็น Word/PDF ต้องลบ)
- ❌ Markdown links/syntax — ตอน Export ต้องแปลงเป็น plain text + เลขหน้า

### 5. ความถูกต้องของข้อมูล (Verification)

- ข้อมูลเชิงเทคนิคในรายงานต้อง **ตรงกับระบบจริง** — ตรวจกับ `doc/modules/`, `doc/architecture/database-api-reference.md`
- ตัวเลขต้องถูกต้อง: 8 Modules, 23 Controllers, ~215 Endpoints, 37 Entities, 53 Migrations, 2 SignalR Hubs
- **ก่อนเขียนตัวเลขในรายงาน** — เช็คกับเอกสารปัจจุบันใน `doc/` (ไม่ใช่จำจาก memory)
- ถ้าตัวเลขใน `doc/` ขัดแย้งกัน → ถามผู้ใช้ว่าอันไหนถูก

### 6. คงรูปแบบเดิมตลอด

- ใช้ pattern เดียวกันทุกบท (ตามที่กำหนดใน `doc/references/`)
- ใช้คำศัพท์เทคนิคที่แปลแล้วใน [writing-style-guide.md](doc/references/writing-style-guide.md) ตลอด
- ถ้าเลือกคำแปลใดในบทแรก → ใช้คำเดียวกันตลอดทั้งรายงาน (ห้ามสลับ "การยืนยันตัวตน" กับ "การพิสูจน์ตัวตน")

---

## 📚 ข้อมูลโปรเจค (สรุปสำหรับใช้อ้างอิง)

### ตัวเลขสำคัญ
| รายการ | จำนวน |
|--------|-------|
| Business Modules | 8 |
| API Controllers | 23 |
| API Endpoints | ~215 |
| Database Entities | 37 |
| Database Migrations | 53 |
| SignalR Hubs | 2 (OrderHub + NotificationHub) |
| Frontend Admin Routes | 40+ |
| Frontend Mobile Routes | 11 |

### Tech Stack ทางการ
- **Backend**: ASP.NET Core 9.0 + Entity Framework Core 9 + SignalR
- **Frontend**: Angular 19.1 + PrimeNG + Tailwind CSS 3.4
- **Database**: SQL Server 2022
- **Storage**: MinIO (S3-compatible)
- **Auth**: JWT + Refresh Token + BCrypt
- **DevOps**: Docker + Docker Compose + Nginx

### 5 บทบาทผู้ใช้
1. เจ้าของร้าน/ผู้จัดการ (Owner/Manager)
2. แคชเชียร์ (Cashier)
3. พนักงานครัว (Kitchen Staff)
4. พนักงานเสริฟ (Waiter)
5. ลูกค้า (Customer Self-Order)

### Innovation หลัก
- Self-Order ผ่าน QR Code
- SignalR Real-time (latency < 1 วินาที)
- OCR Slip Verification
- State Machine ละเอียด (OrderItem 7 สถานะ)
- RBAC Position-based + Permission Matrix
- Multi-Device Protection (Bill Claim Token)

---

## 🚨 ก่อนเริ่มทุกครั้ง

1. **ถ้าผู้ใช้สั่งงานเขียน/แก้รายงาน** → **อ่าน [doc/report/09-questions-for-author.md](doc/report/09-questions-for-author.md) ก่อนเสมอ** แล้วใช้คำตอบในไฟล์เป็น Source of Truth
2. **ถ้าผู้ใช้สั่งงานพัฒนาโค้ด** → **ห้ามทำเลย** ถามผู้ใช้ก่อนว่า:
   > "ขณะนี้อยู่ในโหมดเขียนรายงานปริญญานิพนธ์ (ตาม CLAUDE.md) — หากต้องการกลับโหมดพัฒนาโค้ดให้แจ้งสลับโหมดก่อน หรือยืนยันว่าจะ override โหมดเฉพาะครั้งนี้"
3. **ทุกการแก้ไขเนื้อหารายงาน** ที่กระทบกับคำตอบของผู้ใช้ → ต้องสะท้อนกลับไปในไฟล์ 09 ด้วย เพื่อให้ Source of Truth ตรงกับเนื้อหาจริง

---

## 🔄 การสลับโหมด

### กลับไปโหมดพัฒนาโค้ด
- ผู้ใช้บอก: "**กลับโหมด dev**" หรือ "**สลับเป็นพัฒนาโค้ด**"
- AI: คัดลอก `CLAUDE-dev.md` → `CLAUDE.md` (replace), แจ้งผู้ใช้

### อยู่ในโหมดเขียนรายงาน
- ค่าเริ่มต้น — ทำตามไฟล์นี้
